services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: transit
      POSTGRES_PASSWORD: transit_secret
      POSTGRES_DB: open_transit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U transit -d open_transit"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ../server
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://transit:transit_secret@db:5432/open_transit
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ..:/workspaces/open-transit:cached
    working_dir: /workspaces/open-transit/server
    command: uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
